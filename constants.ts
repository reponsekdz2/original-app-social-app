

// This file is now primarily for type exports if needed, as all mock data is being moved to the backend.
// Fix: Add file extension to import path.
import type { User, Post, Comment, SponsoredContent, FeedActivity, Testimonial, HelpArticle } from './types';
